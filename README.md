# windows-registry-monitor
A Python-based Windows Registry Change Monitoring System that detects startup persistence and security policy modifications using baseline comparison and continuous monitoring.
